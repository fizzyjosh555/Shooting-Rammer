using UnityEngine;
using System.Collections;

public class Aspect : MonoBehaviour {
	public enum aspect {
		Player,
		Enemy,
		Tree,
		healpad,
		locpoints,
		decorations
	}
	public aspect aspectName;
}
